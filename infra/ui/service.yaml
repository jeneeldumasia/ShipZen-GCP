apiVersion: v1
kind: Service
metadata:
  name: shipzen-ui
  namespace: shipzen-system
  labels:
    app: shipzen-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: shipzen-ui
