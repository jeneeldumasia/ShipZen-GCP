apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
    - name: shipzen-gateway
      namespace: shipzen-system
  hostnames:
    - "argocd-shipzen.jeneeldumasia.codes"
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
