apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Teardown mode: keep only system namespaces and prune all runtime resources.
resources:
  - ../system/namespace.yaml
  - ../system/build-namespace.yaml
